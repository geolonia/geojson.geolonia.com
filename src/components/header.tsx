import React from "react";

export const Header: React.FC = () => {
  return (
    <header>
      <h1>{"GeoJSON Editor"}</h1>
      <button>{"Clear"}</button>
    </header>
  );
};

export default Header;
